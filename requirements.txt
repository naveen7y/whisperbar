# Pinned for reproducible installs and supply-chain safety. To update a
# dependency, bump its version here deliberately and re-test, rather than
# floating to whatever is latest. Versions reflect a known-good install.
rumps==0.4.0
sounddevice==0.5.5
numpy==2.0.2
faster-whisper==1.2.1
pynput==1.8.2               # LGPLv3 — see THIRD-PARTY-LICENSES.md
pyobjc-framework-Cocoa==11.1
pyobjc-framework-Quartz==11.1
